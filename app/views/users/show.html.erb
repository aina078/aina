<h1>登録情報</h1>
<p>名前 : <%= @user.name %></p>
<p>メールアドレス : <%= @user.email %></p>
<p>プロフィール : <%= @user.profile %></p>

<% if current_user.id == @user.id %>
  <%= link_to "編集する", edit_user_registration_path %>
<% end %>

<h2>ユーザーの投稿一覧</h2>
<% @user.okashis.each do |t| %>
  <%= t.user.name %>
  <%= t.name %>
  <%= t.genre %>
  <%= t.kansou %>
  <%= t.item %>
  <%= t.place %>
  <%= t.company %>
<% end %>

<!-- 以下はいいね機能を実装している場合-->




<h2>ユーザーがいいねした投稿一覧</h2>
<% @user.liked_okashis.each do |t| %>
  <%= t.user.name %>
  <%= t.body %>
<% end %>
<!-- ここまで-->
<%= image_tag @user.image_url, size: "250x200" if @user.image? %>
<%= link_to "ホームに戻る", okashis_path %>